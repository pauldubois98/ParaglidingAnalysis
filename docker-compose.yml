services:
  web:
    container_name: paragliding
    build: .
    expose:
      - 108
    restart: unless-stopped
    networks:
      - venerqueserver
      - ionosserver

networks:
  venerqueserver:
    external: true
    name: venerqueserver-network
  ionosserver:
    external: true
    name: ionos-internal

services:
  web:
    container_name: paragliding
    build: .
    expose:
      - 108
    restart: unless-stopped
    networks:
      - ionosserver

networks:
  ionosserver:
    external: true
    name: ionos-internal
